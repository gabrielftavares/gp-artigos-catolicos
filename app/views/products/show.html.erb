<div class="container mt-5">
  <%= cl_image_tag @product.photo.key, width: 400, height: 300, crop: :fill  %>
  <h2><%= @product.name %></h2>
  <p><%= @product.description %></p>
  <p><%= @product.category.name %></p>
  <p>R$<%= @product.price.to_i %></p>
</div>
